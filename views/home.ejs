<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate Generator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: "Poppins", sans-serif;
            box-sizing: border-box;
        }

        :root {
            --primary-color: linear-gradient(to right, #0c356a, #0174be);
            --secondary-color: #fff0ce;
            --nav-text: #000000;
            --underline-color: #ffc436;
            --bg: #f8f6f4;
        }

        .dark-theme {
            --secondary-color: #fff;
            --primary-color: #080808;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background: var(--primary-color);
            color: var(--secondary-color);
        }

        .logo img {
            height: 75px;
            width: 75px;
            border-radius: 50%;
        }

        #name {
            justify-content: center;
            display: flex;
            position: relative;
            padding: 10px;
            margin-top: 50px;
        }
        .generator{
            position: relative;
            display: flex;
            justify-content: center;
            margin: 10px;
            padding: 20px;
        }
        #btn{
            padding: 4px;
            justify-content: space-between;
            border-radius: 25%;
            box-shadow: #000000;
            font-family: cursive;
            background-color: #ffc436;
        }
        #inpt{
            padding: 4px;
            border-radius: 10%;
            font-family: Georgia, 'Times New Roman', Times, serif;
        }
    </style>

</head>

<body>
    <div class="hero">
        <h1 id="name">Paridhi Certificate Generator</h1>
    </div>
    
    <div class="generator">
        <form action="/text.ejs" method="post">
            <input type="text" placeholder="Enter Name" name="Name" id="inpt">
            <button type="submit" id="btn"> Enter</button>
        </form>
    </div>
    
</body>

</html>